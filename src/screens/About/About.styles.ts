import { StyleSheet } from "react-native";

const aboutStyles = StyleSheet.create({
  text: {
    color: "#fff",
    fontSize: 20,
    textAlign: "center",
    marginTop: 15,
  },
});

export default aboutStyles;
