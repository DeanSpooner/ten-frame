import { StyleSheet } from "react-native";

export default appStyles = StyleSheet.create({
  container: {
    flex: 1,
    background: "#045d1a",
    alignItems: "center",
    justifyContent: "center",
  },
});
