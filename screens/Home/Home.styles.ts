import { StyleSheet } from "react-native";

const homeStyles = StyleSheet.create({
  text: {
    color: "#fff",
    fontSize: 30,
    textAlign: "center",
    marginHorizontal: 5,
  },
});

export default homeStyles;
